package com.augmentum.masterchef.service;

import org.apache.commons.logging.Log;
import org.apache.commons.logging.LogFactory;

import org.springframework.stereotype.Service;


@Service
public class UserWalletServiceImpl extends UserWalletServiceBaseImpl
    implements UserWalletService {
    private static Log log = LogFactory.getLog(UserWalletServiceImpl.class);
}
