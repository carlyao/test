package com.augmentum.masterchef.service;

import org.apache.commons.logging.Log;
import org.apache.commons.logging.LogFactory;

import org.springframework.stereotype.Service;


@Service
public class Wallet_DefServiceImpl extends Wallet_DefServiceBaseImpl
    implements Wallet_DefService {
    private static Log log = LogFactory.getLog(Wallet_DefServiceImpl.class);
}
