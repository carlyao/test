ant-version=1.6.5
ant.dir=${lib.dir}/ant

castor-xml-version=1.3
castor.dir=${lib.dir}/castor

commons-dbcp-version=1.2.2

database.dir=${lib.dir}/database

ehcache-version=1.3.0
ehcache.dir=${lib.dir}/ehcache

freemarker-version=2.3.12
freemarker.dir=${lib.dir}/freemarker

hibernate-version=3.3.0
hibernate.dir=${lib.dir}/hibernate

j2ee.dir=${lib.dir}/j2ee

jakarta-commons.dir=${lib.dir}/jakarta-commons

jalopy-version=1.5 RC3
jalopy.dir=${lib.dir}/jalopy

javamail-version=1.4
javamail.dir=${lib.dir}/javamail

jaxen-version=1.1.2
jaxen.dir=${lib.dir}/jaxen

jboss.dir=${lib.dir}/jboss

junit-version=4.5
junit.dir=${lib.dir}/junit

logging-version=1.2.15
logging.dir=${lib.dir}/logging

memcached-version=2.0.1
memcached.dir=${lib.dir}/memcached

qdox-version=1.7.0
qdox.dir=${lib.dir}/qdox

quartz-version=1.6.0
quartz.dir=${lib.dir}/quartz

spring-version=2.5.6
spring.dir=${lib.dir}/spring

velocity-version=1.4
velocity.dir=${lib.dir}/velocity

xml.dir=${lib.dir}/xml

json.dir=${lib.dir}/json

facebook-version=2.1.0
facebook.dir=${lib.dir}/facebook

urlrewrite-version=3.2.0
urlrewrite.dir=${lib.dir}/urlrewrite
cglib-version=2.1.3
cglib.dir=${lib.dir}/cglib

testng-version=5.9
testng.dir=${lib.dir}/testng

aspect.dir=${lib.dir}/aspect

httpclient.dir=${lib.dir}/httpclient

cobertura.dir=${lib.dir}/cobertura

struts2-version=2.1.8.1
struts2.dir=${lib.dir}/struts2

selenium.dir=${lib.dir}/selenium
